/** Vercel serverless budget for document analysis (seconds). */
export const ANALYZE_MAX_DURATION_SEC = 300;

/** Abort client fetches slightly before the server limit (ms). */
export const ANALYZE_CLIENT_TIMEOUT_MS = 290_000;
