-- Superseded by 0026_fix_owner_member_rls.sql.
-- Kept so migration history stays ordered. Safe to re-run (no-op if 0026 applied).
-- If you already ran this file with SET row_security = off, run 0026 next.

select 1;
